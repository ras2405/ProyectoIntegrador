export interface IText {
  text: string;
  type: string;
  user: string;
  projectName: string;
  timestamp: string;
  stage: string;
  tag1: number;
  tag2: number;
}

export interface ITag {
  tag: string;
  // color: string;
}
