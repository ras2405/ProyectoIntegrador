export interface IText {
    text: string;
    type: string
    user: string
    projectName: string;
    timestamp: string;
    stage: string;
    tag: string; 
}

export interface ITag {
    tag: string;
    // color: string;
}